
  # Design live stream graphics

  This is a code bundle for Design live stream graphics. The original project is available at https://www.figma.com/design/t6kB9N4CIhYNOKhbUT1urG/Design-live-stream-graphics.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  